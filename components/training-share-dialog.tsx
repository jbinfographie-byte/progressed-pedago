'use client';
/* eslint-disable @next/next/no-img-element -- le QR code est servi dynamiquement par une route privée de l’application */

import { FormEvent, useCallback, useEffect, useState } from 'react';

type ApiResult<T>={ok:true;data:T}|{ok:false;error:{message:string}};
type Share={id:string;trainingId:string;shortCode:string;mode:'classroom'|'home';identityMode:'name'|'pseudonym'|'learner_code'|'anonymous';status:'active'|'disabled';sessionOpen:boolean;expiresAt:number|null;maxAccesses:number|null;accessCount:number;participantCount:number;url:string};
type ShareTraining={id:string;name:string;status:'draft'|'ready'|'published'|'archived';pathStatus:'draft'|'published'|null;activityCount:number};
type Participant={id:string;displayName:string;progressPercent:number;lastSeenAt:number;completedAt:number|null};

async function request<T>(url:string,options?:RequestInit):Promise<T>{const response=await fetch(url,options);const payload=await response.json() as ApiResult<T>;if(!payload.ok)throw new Error(payload.error.message);return payload.data;}
async function copyText(value:string){if(navigator.clipboard?.writeText){await navigator.clipboard.writeText(value);return;}const field=document.createElement('textarea');field.value=value;field.style.position='fixed';field.style.opacity='0';document.body.appendChild(field);field.select();document.execCommand('copy');field.remove();}

export function TrainingShareDialog({training,onClose,onPublish,onManageActivities,notify}:{training:ShareTraining;onClose:()=>void;onPublish:()=>Promise<void>;onManageActivities:()=>void;notify:(message:string)=>void}){
  const[shares,setShares]=useState<Share[]>([]);const[selectedId,setSelectedId]=useState('');const[busy,setBusy]=useState(false);const[loading,setLoading]=useState(true);const[fullscreen,setFullscreen]=useState(false);const[participants,setParticipants]=useState<Participant[]>([]);
  const selected=shares.find((share)=>share.id===selectedId)??shares[0]??null;
  const ready=training.activityCount>0&&training.status==='published'&&training.pathStatus==='published';
  const load=useCallback(async()=>{setLoading(true);try{const data=await request<{shares:Share[]}>(`/api/shares?trainingId=${encodeURIComponent(training.id)}`);setShares(data.shares);setSelectedId((current)=>data.shares.some((share)=>share.id===current)?current:data.shares[0]?.id??'');}catch(reason){notify(reason instanceof Error?reason.message:'Partages indisponibles.');}finally{setLoading(false);}},[training.id,notify]);

  useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer);},[load]);
  useEffect(()=>{const close=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose();};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close);},[onClose]);
  useEffect(()=>{if(!selected?.id)return;let active=true;const refresh=()=>request<{participants:Participant[]}>(`/api/shares/${selected.id}/participants`).then((data)=>{if(active)setParticipants(data.participants);}).catch(()=>{});const initial=window.setTimeout(()=>void refresh(),0);const timer=window.setInterval(refresh,10000);return()=>{active=false;window.clearTimeout(initial);window.clearInterval(timer);};},[selected?.id]);

  const create=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setBusy(true);const form=new FormData(event.currentTarget);try{const result=await request<{message:string}>('/api/shares',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({trainingId:training.id,mode:form.get('mode'),identityMode:form.get('identityMode'),expiresAt:form.get('expiresAt'),maxAccesses:Number(form.get('maxAccesses'))||null,sessionOpen:true})});notify(result.message);await load();}catch(reason){notify(reason instanceof Error?reason.message:'Création impossible.');}finally{setBusy(false);}};
  const update=async(patch:Record<string,unknown>)=>{if(!selected)return;setBusy(true);try{const result=await request<{message:string}>(`/api/shares/${selected.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(patch)});notify(result.message);await load();}catch(reason){notify(reason instanceof Error?reason.message:'Modification impossible.');}finally{setBusy(false);}};
  const publish=async()=>{setBusy(true);try{await onPublish();}finally{setBusy(false);}};

  return <div className={`modal-backdrop share-modal-backdrop ${fullscreen?'qr-fullscreen':''}`} role="presentation" onMouseDown={(event)=>{if(event.target===event.currentTarget)onClose();}}><section className="modal-card training-share-dialog" role="dialog" aria-modal="true" aria-labelledby="share-dialog-title"><button className="modal-close" type="button" onClick={onClose} aria-label="Fermer la fenêtre de partage">×</button><p className="overline">Partager avec les apprenants</p><h2 id="share-dialog-title">{training.name}</h2>
    {loading?<div className="share-loading" role="status"><span/><strong>Chargement des options de partage…</strong></div>:selected?<ShareDetails selected={selected} training={training} participants={participants} busy={busy} fullscreen={fullscreen} setFullscreen={setFullscreen} update={update} notify={notify}/>:ready?<ShareCreateForm busy={busy} create={create}/>:<ShareReadiness training={training} busy={busy} onPublish={publish} onManageActivities={onManageActivities} onClose={onClose}/>}
  </section></div>;
}

function ShareReadiness({training,busy,onPublish,onManageActivities,onClose}:{training:ShareTraining;busy:boolean;onPublish:()=>Promise<void>;onManageActivities:()=>void;onClose:()=>void}){const hasActivities=training.activityCount>0;const published=training.status==='published'&&training.pathStatus==='published';return <section className="share-readiness"><div className="share-readiness-intro"><span>✓</span><div><strong>La fenêtre de partage fonctionne.</strong><p>Préparez maintenant ce parcours avant de générer le lien et le QR code.</p></div></div><div className="share-readiness-checks"><article className={hasActivities?'done':'todo'}><span>{hasActivities?'✓':'1'}</span><div><strong>Ajouter au moins une activité</strong><small>{hasActivities?`${training.activityCount} activité${training.activityCount>1?'s':''} dans ce parcours`:'Le parcours est actuellement vide.'}</small></div></article><article className={published?'done':'todo'}><span>{published?'✓':'2'}</span><div><strong>Publier le parcours</strong><small>{published?'Le parcours est prêt à être diffusé.':'La publication active l’accès apprenant sécurisé.'}</small></div></article></div><div className="modal-actions"><button className="button light" type="button" onClick={onClose}>Fermer</button>{!hasActivities?<button className="button dark" type="button" onClick={onManageActivities}>Ajouter des activités</button>:!published?<button className="button dark" type="button" disabled={busy} onClick={()=>void onPublish()}>{busy?'Publication…':'Publier et préparer le partage'}</button>:null}</div></section>;}

function ShareCreateForm({busy,create}:{busy:boolean;create:(event:FormEvent<HTMLFormElement>)=>Promise<void>}){return <form className="share-create-form" onSubmit={(event)=>void create(event)}><p>Créez un accès direct sans adresse électronique ni mot de passe.</p><div className="form-row"><label>Mode<select name="mode"><option value="classroom">Session en salle</option><option value="home">Travail à domicile</option></select></label><label>Informations demandées<select name="identityMode"><option value="name">Prénom et nom</option><option value="pseudonym">Pseudonyme</option><option value="learner_code">Code apprenant</option><option value="anonymous">Aucune information</option></select></label></div><div className="form-row two"><label>Date d’expiration<input type="datetime-local" name="expiresAt"/></label><label>Nombre maximal d’accès<input type="number" name="maxAccesses" min="1" max="10000" placeholder="Illimité"/></label></div><button className="button dark" disabled={busy}>{busy?'Création…':'Générer le lien et le QR code'}</button></form>;}

function ShareDetails({selected,training,participants,busy,fullscreen,setFullscreen,update,notify}:{selected:Share;training:ShareTraining;participants:Participant[];busy:boolean;fullscreen:boolean;setFullscreen:(value:boolean|((current:boolean)=>boolean))=>void;update:(patch:Record<string,unknown>)=>Promise<void>;notify:(message:string)=>void}){return <><div className="share-status-strip"><span className={selected.status}>{selected.status==='active'?'Lien actif':'Lien désactivé'}</span><strong>{selected.mode==='classroom'?'Session en salle':'Travail à domicile'}</strong><small>{selected.accessCount} accès · {participants.length} participant{participants.length>1?'s':''}</small></div><div className="share-content"><section className="share-qr"><img src={`/api/shares/${selected.id}/qr`} alt={`QR code de la formation ${training.name}`}/><strong>Code court : {selected.shortCode}</strong><button className="button light" type="button" onClick={()=>setFullscreen((value)=>!value)}>{fullscreen?'Réduire':'Afficher en plein écran'}</button></section><section className="share-controls"><label>Lien sécurisé<div><input readOnly value={selected.url}/><button type="button" onClick={async()=>{try{await copyText(selected.url);notify('Lien copié.');}catch{notify('Copie impossible : sélectionnez le lien puis copiez-le manuellement.');}}}>Copier</button></div></label><div className="share-downloads"><a className="button light" href={`/api/shares/${selected.id}/qr?format=png`}>Télécharger le PNG</a><a className="button light" href={`/api/shares/${selected.id}/qr?format=pdf`}>Fiche PDF A4</a><button className="button light" type="button" onClick={()=>window.print()}>Imprimer</button></div>{selected.mode==='classroom'&&<label className="session-switch"><input type="checkbox" checked={selected.sessionOpen} onChange={(event)=>void update({sessionOpen:event.target.checked})}/><span><strong>{selected.sessionOpen?'Session ouverte':'Session fermée'}</strong><small>Les apprenants {selected.sessionOpen?'peuvent entrer maintenant':'ne peuvent plus entrer'}</small></span></label>}<div className="share-danger-actions"><button className="button light" disabled={busy} type="button" onClick={()=>void update({renew:true})}>Renouveler le lien</button>{selected.status==='active'?<button className="button light danger-text" disabled={busy} type="button" onClick={()=>void update({status:'disabled'})}>Désactiver</button>:<button className="button dark" disabled={busy} type="button" onClick={()=>void update({status:'active'})}>Réactiver</button>}</div></section></div><section className="live-participants"><div><p className="overline">Suivi en direct</p><h3>{participants.length} participant{participants.length>1?'s':''}</h3></div>{participants.length?<div>{participants.slice(0,8).map((participant)=><article key={participant.id}><span>{participant.displayName.slice(0,2).toUpperCase()}</span><div><strong>{participant.displayName}</strong><small>Vu à {new Date(participant.lastSeenAt*1000).toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})}</small></div><b>{participant.progressPercent}%</b></article>)}</div>:<p>Les participants apparaîtront ici dès leur entrée dans le parcours.</p>}</section></>;}
